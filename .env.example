# GoUrls Development Environment Configuration Example
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in your project root
# 2. Customize the values below as needed for your environment
# 3. The startup.sh script will automatically load these settings
# 
# SECURITY NOTE:
# Never commit .env with real passwords to git!
# This .env.example file shows the structure with safe example values.

# =============================================================================
# 🌐 Application & Service Configuration
# =============================================================================

# Project name (used for container names, URLs, etc.)
# This affects Docker container naming and internal references
PROJECT_NAME=gourls

# Domain for Go Links (without protocol)
# This should match your hosts file entry (e.g., 127.0.0.1 go)
GO_DOMAIN=go

# =============================================================================
# 🔌 Port Configuration
# =============================================================================

# nginx reverse proxy port (typically 80 for go/ domain to work properly)
# WARNING: Port 80 requires sudo/admin privileges
NGINX_PORT=80

# Angular development server port
# Change if 4200 conflicts with other services
ANGULAR_PORT=4200

# .NET API server port  
# Change if 5165 conflicts with other services
API_PORT=5165

# PostgreSQL database port (host port mapped to container)
# This is the external port, container always uses 5432 internally
POSTGRES_PORT=5431

# =============================================================================
# 🐘 Database Configuration
# =============================================================================

# Database connection details
POSTGRES_HOST=127.0.0.1
POSTGRES_DB=gourls
POSTGRES_USER=postgres

# Database password - CHANGE THIS IN PRODUCTION!
POSTGRES_PASSWORD=password123

# Docker container name for PostgreSQL
# Change if you want a different container name
POSTGRES_CONTAINER_NAME=gourls-postgres

# PostgreSQL Docker image version
# Pin to specific version for consistency
POSTGRES_VERSION=15

# =============================================================================
# 📁 Directory & File Paths
# =============================================================================

# Logs directory for development scripts
# Relative to project root
LOGS_DIR=logs

# nginx configuration file path
# The startup script can generate this from template
NGINX_CONFIG_FILE=nginx-go-proxy.conf

# =============================================================================
# 🔧 Development & Build Configuration
# =============================================================================

# Node.js version requirement (for Angular 20+)
# Used for version checking and nvm management
NODE_VERSION_REQUIRED=22.21.1

# .NET project directory
# Relative to project root
API_PROJECT_DIR=GoUrlsApi

# Angular project directory
# Relative to project root
ANGULAR_PROJECT_DIR=go-urls-app

# =============================================================================
# 🚀 Service URLs (Generated from above config)
# =============================================================================

# These are automatically constructed from the above values
# No need to modify unless you have specific requirements

# Full service URLs for health checks and documentation
ANGULAR_URL=http://localhost:${ANGULAR_PORT}
API_URL=http://localhost:${API_PORT}
API_HEALTH_URL=http://localhost:${API_PORT}/api/urls
GO_URL=http://${GO_DOMAIN}

# Database connection string (PostgreSQL format)
# This will be used by the .NET application
DB_CONNECTION_STRING=Host=${POSTGRES_HOST};Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}