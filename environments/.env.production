# 🐳 Container Production Configuration
# This file configures CONTAINERIZED production-like environment
# Can run in parallel with local development

# Project Settings
PROJECT_NAME=gourls
GO_DOMAIN=go

# 🏭 PRODUCTION PORTS (Container processes)
NGINX_PORT=80                 # Standard HTTP port
FRONTEND_PORT=8080            # Angular container
API_PORT=5000                 # .NET Core API container
POSTGRES_PORT=5432            # PostgreSQL container (internal)

# 🌐 PRODUCTION URLs 
MAIN_URL=http://go            # Main access via nginx
FRONTEND_URL=http://localhost:8080
API_URL=http://localhost:5000

# 📊 Database Configuration (Production-like)
POSTGRES_HOST=postgres        # Container hostname
POSTGRES_DB=gourls            # Production database name
POSTGRES_USER=postgres
POSTGRES_PASSWORD=gourls_secure_password
POSTGRES_VERSION=15

# 🔗 API Configuration
API_BASE_URL=http://api:5000

# 🏗️ Build Configuration
NODE_ENV=production
ASPNETCORE_ENVIRONMENT=Production

# 🐳 Container-specific settings
COMPOSE_PROJECT_NAME=gourls-production
RESTART_POLICY=unless-stopped
CONTAINER_MODE=true