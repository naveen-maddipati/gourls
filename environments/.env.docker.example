# GoUrls Docker Environment Configuration Template
# Copy this file to .env.docker and customize for your deployment

# =============================================================================
# 🐳 Docker Compose Configuration
# =============================================================================

# Project name (used for container naming)
COMPOSE_PROJECT_NAME=gourls

# =============================================================================
# 🔌 Port Configuration  
# =============================================================================

# nginx reverse proxy port (main entry point)
NGINX_PORT=80

# Frontend direct access port
FRONTEND_PORT=8080

# API direct access port  
API_PORT=5000

# PostgreSQL database port
POSTGRES_PORT=5432

# =============================================================================
# 🐘 Database Configuration
# =============================================================================

# Database connection details
POSTGRES_DB=gourls
POSTGRES_USER=postgres

# ⚠️  IMPORTANT: Change this password for production!
POSTGRES_PASSWORD=your_secure_password_here

# =============================================================================
# 🔧 Application Configuration
# =============================================================================

# .NET Environment
ASPNETCORE_ENVIRONMENT=Production

# =============================================================================
# 🚀 Production Deployment Options
# =============================================================================

# Uncomment and customize for production:

# Use HTTPS (requires SSL certificates)
# NGINX_PORT=443

# Custom domain name
# DOMAIN_NAME=go.yourcompany.com

# Enable additional security features
# ENABLE_SSL=true
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Database performance tuning
# POSTGRES_MAX_CONNECTIONS=100
# POSTGRES_SHARED_BUFFERS=256MB